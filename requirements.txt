# Paquetes principales (Python 3.11 optimizado)
requests==2.31.0
psycopg2-binary==2.9.9
python-dotenv==1.0.0
qrcode[pil]==7.4.2

# Dependencias de Google
google-auth==2.23.0
google-auth-oauthlib==1.0.0
google-auth-httplib2==0.1.0
google-api-python-client==2.24.0

# Flask y extensiones (versiones compatibles)
Flask==2.3.2                          # Downgrade para compatibilidad con Flask-Login 0.5.0
Werkzeug==2.3.7                       # Versión compatible con Flask 2.3.2
Flask-Cors==4.0.0
Flask-Login==0.5.0                    # Mantenemos esta versión (requiere Flask <3.0)
Flask-Babel==3.1.0

# Seguridad y numéricos
cryptography==41.0.7
numpy==1.26.0

# Traducción (versión estable alternativa)
deep-translator==1.11.4               # Reemplazo confiable para googletrans